<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ecommerceclone">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcommerceClone">
        <activity
            android:name=".VerifyOtp"
            android:exported="false" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".ProceedToCout"
            android:exported="false" />
        <activity
            android:name=".CategoryWise"
            android:exported="false" />
        <activity android:name=".ProductDescription" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.EcommerceClone.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Homepage"
            android:label="@string/title_activity_homepage"
            android:theme="@style/Theme.EcommerceClone.NoActionBar" />
        <activity android:name=".Register" />
        <activity android:name=".Loginpage" />
        <activity
            android:name=".SettingsActivity"
            android:theme="@style/Theme.EcommerceClone.NoActionBar" />
    </application>

</manifest>